package main

import (
	"testing"

	_ "github.com/mattn/go-sqlite3"
)

func TestData(t *testing.T) {
	// is := is.New(t)
	// is.True(true)
	// ctx := context.Background()
	// _ = ctx
	// store := actorstore.ActorStore{Dir: "./testdata/data", ReadOnly: true}
	// did := syntax.DID("did:plc:kzvsijt4365vidgqv7o6wksi")
	// _, _ = store, did
	// // prefstore, err := store.Pref(did)
	// // is.NoErr(err)
	// // prefs, err := prefstore.List()
	// rr, err := store.Record(did)
	// is.NoErr(err)
	// collections, err := rr.ListCollections(ctx)
	// is.NoErr(err)
	// for c, err := range collections {
	// 	if err != nil {
	// 		t.Error(err)
	// 		break
	// 	}
	// 	fmt.Println(c)
	// }
}
